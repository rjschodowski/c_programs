/*

9. Write a C program to check whether two given integer values are in the range 20..50 inclusive. Return true if 1 or other is in the said range otherwise false. 
Expected Output:

1
1
1
0

*/


#include <stdio.h>

int main(void)
{
  printf("%d\n", verify(22, 41));
  printf("%d\n", verify(50, 36));
  printf("%d\n", verify(22,1));
  printf("%d\n", verify(19, 5));
}

int verify(int a, int b) 
{
  return (a >= 20 && a <= 50) || (b >= 20 && b <= 50);
}

//This is a variation of the previous challenge (W3Algo8).
