/*
Write a program in C to sort an array using Pointer.

Test Data :
Input the number of elements to store in the array : 5
Input 5 number of elements in the array :
element - 1 : 25
element - 2 : 45
element - 3 : 89
element - 4 : 15
element - 5 : 82
Expected Output :
                       
 The elements in the array after sorting :                                                                    
 element - 1 : 15                                                
 element - 2 : 25                                                
 element - 3 : 45                                   
 element - 4 : 82                                            
 element - 5 : 89  
*/

#include <stdio.h>

int main() {
  int arr[50], counter, total_count;
  //Get total numbers to sort
  printf("How many numbers do you want to input?\n");
  scanf("%d", &total_count);

  //Get numbers that will be sorted in ascending order

  for(counter = 0; counter < total_count; counter++) {
    printf("Enter a number: ");
    scanf("%d", &arr[counter]);
  }
  printf("Here are your numbers:\n");
  for(counter = 0; counter < total_count; counter++) {
    printf("%d\n", arr[counter]);
  }
  

} //Please note I am still working through the logic and testing to ensure the program scans and prints integers in the order entered before writing a sort algorithm.
